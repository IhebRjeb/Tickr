/**
 * Currency VO - Re-exported from @shared/domain/value-objects/currency.vo
 *
 * This file exists for backward compatibility. The canonical source
 * is now in @shared since multiple modules (Events, Tickets, Payments) use it.
 */
export {
  Currency,
  CurrencyVO,
  CURRENCY_METADATA,
} from '@shared/domain/value-objects/currency.vo';

export type { CurrencyMetadata } from '@shared/domain/value-objects/currency.vo';

